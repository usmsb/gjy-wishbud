# Client修改的地方

## 一、增加快捷方式

### 文件 `client/src/components/Chat/ChatView.tsx`

0. 在 `import { buildTree, cn } from '~/utils';`和`import { Button } from '../ui';`之间
```
import { buildTree, cn } from '~/utils';

import { useQueryClient } from '@tanstack/react-query'
import { addConversation, generateUUID } from "~/utils"
import { ConversationData, QueryKeys } from "~/data-provider/data-provider/src"

import { Button } from '../ui';

```

1. 在 如下  `import Presentation from './Presentation';`和`const ChatView = ({ index = 0 }: { index?: number }) => {` 之间增加代码：

```
import MessagesView from './Messages/MessagesView';
import Presentation from './Presentation';

// 开始增加内容：

//  修改后的 QuickAccess 组件
const QuickAccess = () => {
  const quickActions = [
    // {
      // id: 'bb89020ee1ac4d0c8d3b12234d0a85b9',
      // name: '知识问答',
      // description: '基于知识库的智能问答',
      // icon: '🧠',
      // prompt: '请帮我查询相关知识...',
      // type:10
    // },
    {
      id: '440870e48aa8494181c57f59613fe822',
      name: '负面清单审查',
      description: '技术报告负面清单检查',
      icon: '🔍',
      prompt: '请检查这份技术报告...',
      type:10
    },
    {
      id: 'http://192.168.11.203:5000/achievement',
      name: '年度总结',
      description: '自动生成个人年度总结',
      icon: '📊',
      prompt: '请帮我生成年度工作总结...',
      type:20
    },
    {
      id: 'http://192.168.1.153:1051/',
      name: '职业规划',
      description: '技术人员职业发展建议',
      icon: '🚀',
      prompt: '请提供职业发展建议...',
      type:20
    },
    {
      id: '836150bd90b4458dbec79430c9f89ae5',
      name: '策论生成',
      description: '智能生成策论报告',
      icon: '📝',
      prompt: '请帮我撰写关于...的策论报告',
      type:10
    }
  ];

  const handleQuickAction = (action) => {
    // 这里可以设置输入框的值或者直接触发对话
    console.log('快速入口点击:', action.prompt);
    // 你可以根据需要实现具体的逻辑，比如设置输入框内容
    // methods.setValue('text', prompt);
    if(action.type===10)
      handleCardClick(action)
    else
      window.open(action.id, '_blank');
  };

  const { setConversation } = store.useCreateConversationAtom(0);
  const queryClient = useQueryClient();
  const navigate = useNavigate();
  const handleCardClick = (agent) => {
          console.log('agent :>> ', agent);
  
          const _chatId = generateUUID(32)
          const flowId = agent.id
          const flowType = agent.type
          // 新建会话
          queryClient.setQueryData<ConversationData>([QueryKeys.allConversations], (convoData) => {
              if (!convoData) {
                  return convoData;
              }
              setConversation((prevState: any) => {
                  return {
                      ...prevState,
                      conversationId: _chatId
                  }
              })
              return addConversation(convoData, {
                  conversationId: _chatId,
                  createdAt: "",
                  endpoint: null,
                  endpointType: null,
                  model: "",
                  flowId,
                  flowType: flowType,
                  title: agent.name,
                  tools: [],
                  updatedAt: ""
              });
          });
          navigate(`/chat/${_chatId}/${flowId}/${flowType}`);
      }

  return (
    <div className="mt-8 mb-4">
      <p className="text-sm text-center text-gray-500 mb-4">常用快捷入口</p>
      <div className="grid grid-cols-2 md:grid-cols-4 gap-3 max-w-5xl mx-auto px-4">
        {quickActions.map((action) => (
          <div
            key={action.id}
            className="border border-gray-200 rounded-lg p-3 hover:shadow-md transition-all cursor-pointer bg-white/80 backdrop-blur-sm hover:bg-white"
            onClick={() => handleQuickAction(action)}
          >
            {/* 第一行：icon 和 title 并排 */}
            <div className="flex items-center justify-between mb-2">
              <div className="text-xl">{action.icon}</div>
              <h3 className="font-medium text-xs text-left flex-1 ml-2">{action.name}</h3>
            </div>
            {/* 第二行：description */}
            <p className="text-xs text-gray-500 text-left leading-tight">{action.description}</p>
          </div>
        ))}
      </div>
    </div>
  );
};

// 结束增加内容

const ChatView = ({ index = 0 }: { index?: number }) => {
  const { conversationId } = useParams();
  const rootSubmission = useRecoilValue(store.submissionByIndex(index));
  const addedSubmission = useRecoilValue(store.submissionByIndex(index + 1));
  const [showCode, setShowCode] = useState(false);
  const [inputFloat, setInputFloat] = useState(false);
  const [inputWidth, setInputWidth] = useState(0); // To hold the calculated width

```

2. 在 ` <ChatForm isLingsi={isLingsi} setShowCode={setShowCode} index={index} />{!inputFloat && <div className="h-[2vh]"></div>}</div>` 标签与 ` </div>{isLingsi && <Cases />}`之间增加如下内容

```
                    <ChatForm isLingsi={isLingsi} setShowCode={setShowCode} index={index} />
                    {!inputFloat && <div className="h-[2vh]"></div>}
                  </div>

                  {/* 修改位置：在日常模式且没有对话历史时显示快捷入口 */}
                  {!isLingsi && (!messagesTree || messagesTree.length === 0) && (
                    <QuickAccess />
                  )}

                </div>
                {isLingsi && <Cases />}
```

## 二、增加日常模式中的企业知识库问答

### 修改文件：`client/src/pages/appChat/index.tsx`

0. 在 ` const build = useBuild() `和` const init = async () => {`之间加入如下代码：

```

    useConfig()

    // 使用ref标记是否已经处理过自动提交，避免重复执行
    const hasProcessedRef = useRef(false);
    const hasSentInitDataRef = useRef(false); // 标记是否已发送init_data
    const hasSentInputRef = useRef(false); // 标记是否已发送input

    // 自动提交数据（如果有）
    useEffect(() => {
        if (location.state?.autoSubmitData && cid) {
            let initTimer: NodeJS.Timeout | null = null;
            let inputTimer: NodeJS.Timeout | null = null;
            
            const executeAutoSubmit = () => {
                // 如果已经处理过，直接返回
                if (hasProcessedRef.current) return;
                
                const currentChat = chats[cid];
                if (currentChat?.flow) {
                    hasProcessedRef.current = true; // 标记为已处理
                    
                    const autoSubmitData = {
                        ...location.state.autoSubmitData,
                        flow: currentChat.flow // 使用已加载的flow数据
                    };
                    
                    console.log('开始自动提交流程，flow数据已就绪');
                    
                    // 先更新UI状态，给用户即时反馈
                    setRunningState((prev) => ({
                        ...prev,
                        [cid]: {
                            ...prev[cid], // 借鉴useAreaText.ts模式：先展开现有状态，避免undefined错误
                            running: true,
                            showStop: true,
                            showUpload: false,
                            inputDisabled: true,
                            inputForm: false as any, // inputForm类型为any，需要类型断言
                            // error、guideWord、showReRun等属性会自动保留，无需手动设置
                        }
                    }));
                    
                    // 采用更可靠的自动提交策略：先发送init_data，等待足够时间后再发送input
                    // 确保WebSocket有足够时间建立连接
                    if (!hasSentInitDataRef.current) {
                        initTimer = setTimeout(() => {
                            console.log('发送init_data消息');
                            hasSentInitDataRef.current = true;
                            // 发送init_data消息建立会话
                            setSubmitData({
                                action: 'init_data',
                                chatId: cid,
                                flow: currentChat.flow
                            });
                            
                            // 等待更长时间确保WebSocket连接建立和init_data处理完成
                            if (!hasSentInputRef.current) {
                                inputTimer = setTimeout(() => {
                                    console.log('发送input消息');
                                    hasSentInputRef.current = true;
                                    setSubmitData({
                                        action: 'input',
                                        chatId: cid,
                                        flow: currentChat.flow,
                                        input: autoSubmitData.input,
                                        files: autoSubmitData.files
                                    });
                                }, 5000); // 等待5秒确保WebSocket连接和init_data处理完成
                            }
                        }, 200); // 等待500ms确保组件状态稳定
                    }
                    // 清除路由状态，避免重复提交
                    window.history.replaceState({}, document.title);
                    // 清除autoSubmitData标记，从根源上避免重复提交
                    if (location.state?.autoSubmitData) {
                        location.state.autoSubmitData = null;
                    }
                }
            };

            // 检查是否已经完成init
            if (initCompleted[cid]) {
                console.log('init已完成，立即执行自动提交');
                executeAutoSubmit();
                
                // 返回清理函数 - 只清理未发送的定时器
                return () => {
                    // 如果init_data还没发送，清理initTimer
                    if (!hasSentInitDataRef.current && initTimer) {
                        // clearTimeout(initTimer);
                    }
                    // 如果input还没发送，清理inputTimer
                    if (!hasSentInputRef.current && inputTimer) {
                        // clearTimeout(inputTimer);
                    }
                    // 注意：这里不重置hasProcessedRef，只在组件卸载时重置
                };
            } else {
                console.log('等待init完成...');
                // 等待init完成
                const waitForInit = setInterval(() => {
                    if (initCompleted[cid]) {
                        console.log('init已完成，执行自动提交');
                        clearInterval(waitForInit);
                        executeAutoSubmit();
                    }
                }, 100); // 每100ms检查一次

                // 设置超时，防止无限等待
                const timeoutTimer = setTimeout(() => {
                    clearInterval(waitForInit);
                    console.warn('等待init完成超时，尝试直接执行自动提交');
                    executeAutoSubmit();
                }, 10000); // 10秒超时

                // 统一的清理函数 - 只清理未发送的定时器
                return () => {
                    clearInterval(waitForInit);
                    clearTimeout(timeoutTimer);
                    if (!hasSentInitDataRef.current && initTimer) clearTimeout(initTimer);
                    if (!hasSentInputRef.current && inputTimer) clearTimeout(inputTimer);
                };
            }
        }
    }, [cid, location.state?.autoSubmitData, initCompleted]); // 移除chats依赖，避免状态更新导致重新执行

```

1. 在` setChats(prevChats => ({` 和 `setRunningState((prev) => {`之间加入`setInitCompleted`

```
        setChats(prevChats => ({
            ...prevChats,
            [cid]: {
                flow: flowData,
                messages,
                historyEnd: false
            }
        }));

        // 标记init完成 - 必须在setChats之后，确保chats状态已经更新
        setInitCompleted(prev => ({
            ...prev,
            [cid]: true
        }));

        // 更新状态
        // !!flow.data?.nodes.find(node => ["VariableNode", "InputFileNode"].includes(node.data.type))
        setRunningState((prev) => {
```

2. 在 `return <ChatView data={chatState.flow} v={API_VERSION} />};` 和 `const useBuild = () => {` 之间加入
```
const useConfig = () => {
    const [_, setConfig] = useRecoilState(bishengConfState)

    useEffect(() => {
        getBysConfigApi().then(res => {
            setConfig(res.data)
        })
    }, [])
}
```

3. 将`import { useEffect, useState } from "react";`改为`import { useEffect, useRef, useState } from "react";`,加入了`useRef`

4. 将`import { useParams } from "react-router-dom";`改为`import { useLocation, useParams } from "react-router-dom";`,即加入`useLocation`

5. 将`import { getAssistantDetailApi, getChatHistoryApi, getDeleteFlowApi, getFlowApi, postBuildInit } from "~/api/apps";
`改为`import { getAssistantDetailApi, getBysConfigApi, getChatHistoryApi, getDeleteFlowApi, getFlowApi, postBuildInit } from "~/api/apps";
`,即加入`getBysConfigApi`

6. 将`import { chatIdState, chatsState, currentChatState, runningState, tabsState } from "./store/atoms";`改为`import { bishengConfState, chatIdState, chatsState, currentChatState, runningState,submitDataState, tabsState } from "./store/atoms";`,即加入`bishengConfState`、`submitDataState`

7. 在`index`中增加
```
    const location = useLocation();
    const [submitData, setSubmitData] = useRecoilState(submitDataState)
    const [initCompleted, setInitCompleted] = useState<Record<string, boolean>>({})
```


## 三、修复日常模式模型选中的bug
### 修改文件`client/src/components/Chat/Input/ChatForm.tsx`
0. 在`ModelSelect`函数中加入

```
  // 在组件挂载时检查并设置默认值
  useEffect(() => {
    if (options && options.length > 0) {
      const currentOpt = options.find(opt => Number(opt.id) === value)
      // 如果当前值无效（为0或不在选项中），则设置默认值
      if (!currentOpt) {
        const firstOptionId = Number(options[0].id)
        onChange(firstOptionId + '')
      }
    }
  }, [options, value, onChange])

```

即

```
const ModelSelect = ({ options, value, disabled, onChange }: { options?: BsConfig['models'], disabled: boolean, value: number, onChange: (value: string) => void }) => {

  // 在组件挂载时检查并设置默认值
  useEffect(() => {
    if (options && options.length > 0) {
      const currentOpt = options.find(opt => Number(opt.id) === value)
      // 如果当前值无效（为0或不在选项中），则设置默认值
      if (!currentOpt) {
        const firstOptionId = Number(options[0].id)
        onChange(firstOptionId + '')
      }
    }
  }, [options, value, onChange])

  const label = useMemo(() => {
    if (!options) return ''

  ...

```

## 增加企业知识库选项
 修改文件`client/src/components/Chat/Input/ChatFormTools.tsx`
0. 在` <SelectContent className='bg-white rounded-xl p-2 w-52'>`中增加企业知识库选项，即

```
            {
                config?.knowledgeBase.enabled && <div className='flex justify-between'>
                    <div className='flex gap-2 items-center'>
                        <FileText size="16" />
                        <span className="text-xs font-normal">{localize('com_tools_org_knowledge')}</span>
                    </div>
                    <Tooltip delayDuration={200}>
                        <TooltipTrigger >
                            <Switch className='data-[state=checked]:bg-blue-600'
                                disabled={building || disabled}
                                checked={searchType === 'enterpriseKnowledgeSearch'}
                                onCheckedChange={val => {
                                    if (searchType === 'enterpriseKnowledgeSearch') {
                                        setSearchType('');
                                    } else {
                                        setSearchType('enterpriseKnowledgeSearch');
                                    }
                                }}
                            ></Switch>
                        </TooltipTrigger>

                        {building && <TooltipContent
                            className={`text-sm shadow-md`}
                            avoidCollisions={false}
                            sticky="always"
                        >
                            <p>{localize('com_tools_knowledge_rebuilding')}</p>
                        </TooltipContent>
                        }
                    </Tooltip>
                </div>
            }
```

修改后为：

```
<SelectContent className='bg-white rounded-xl p-2 w-52'>
            {
                config?.webSearch.enabled && <div className='flex justify-between mb-3'>
                    <div className='flex gap-2 items-center'>
                        <GlobeIcon className='' size="16" />
                        <span className="text-xs font-normal">联网搜索</span>
                    </div>
                    <Switch className='data-[state=checked]:bg-blue-600'
                        disabled={disabled}
                        checked={searchType === 'netSearch'}
                        onCheckedChange={val => {
                            if (searchType === 'netSearch') {
                                setSearchType('');
                            } else {
                                setSearchType('netSearch');
                            }
                        }}
                    ></Switch>
                </div>
            }
            {
                config?.knowledgeBase.enabled && <div className='flex justify-between mb-3'>
                    <div className='flex gap-2 items-center'>
                        <FileText size="16" />
                        <span className="text-xs font-normal">个人知识库</span>
                    </div>
                    <Tooltip delayDuration={200}>
                        <TooltipTrigger >
                            <Switch className='data-[state=checked]:bg-blue-600'
                                disabled={building || disabled}
                                checked={searchType === 'knowledgeSearch'}
                                onCheckedChange={val => {
                                    if (searchType === 'knowledgeSearch') {
                                        setSearchType('');
                                    } else {
                                        setSearchType('knowledgeSearch');
                                    }
                                }}
                            ></Switch>
                        </TooltipTrigger>

                        {building && <TooltipContent
                            className={`text-sm shadow-md`}
                            avoidCollisions={false}
                            sticky="always"
                        >
                            <p>个人知识库 embedding 模型已更换，正在重建知识库，请稍后再试</p>
                        </TooltipContent>
                        }
                    </Tooltip>
                </div>
            }
            {
                config?.knowledgeBase.enabled && <div className='flex justify-between'>
                    <div className='flex gap-2 items-center'>
                        <FileText size="16" />
                        <span className="text-xs font-normal">{localize('com_tools_org_knowledge')}</span>
                    </div>
                    <Tooltip delayDuration={200}>
                        <TooltipTrigger >
                            <Switch className='data-[state=checked]:bg-blue-600'
                                disabled={building || disabled}
                                checked={searchType === 'enterpriseKnowledgeSearch'}
                                onCheckedChange={val => {
                                    if (searchType === 'enterpriseKnowledgeSearch') {
                                        setSearchType('');
                                    } else {
                                        setSearchType('enterpriseKnowledgeSearch');
                                    }
                                }}
                            ></Switch>
                        </TooltipTrigger>

                        {building && <TooltipContent
                            className={`text-sm shadow-md`}
                            avoidCollisions={false}
                            sticky="always"
                        >
                            <p>{localize('com_tools_knowledge_rebuilding')}</p>
                        </TooltipContent>
                        }
                    </Tooltip>
                </div>
            }
        </SelectContent>
```

设置默认选中组织知识库：
在// 每次重置工具的地方重置为`enterpriseKnowledgeSearch`

```
    // 每次重置工具
    useEffect(() => {
        setSearchType('enterpriseKnowledgeSearch');
    }, [])
```

修改工具显示为选中的内容，即把如下代码进行修改：
```
 return <Select disabled={disabled}>
        <SelectTrigger className="h-7 rounded-full px-2 bg-white dark:bg-transparent data-[state=open]:border-blue-500">
            <div className={cn('flex gap-2', searchType && 'text-blue-600')}>
                <Settings2Icon size="16" />
                <span className="text-xs font-normal">{localize('com_tools_title')}</span>
            </div>
        </SelectTrigger>
```
改为：
```
return <Select disabled={disabled}>
        <SelectTrigger className="h-7 rounded-full px-2 bg-white dark:bg-transparent data-[state=open]:border-blue-500">
            <div className={cn('flex gap-2', searchType && 'text-blue-600')}>
                <Settings2Icon size="16" />
                {/* <span className="text-xs font-normal">{localize('com_tools_title')}</span> */}
                <span className="text-xs font-normal">
                    {searchType === 'netSearch'
                        ? localize('com_tools_web_search')
                        : searchType === 'knowledgeSearch'
                        ? localize('com_tools_personal_knowledge')
                        : searchType === 'enterpriseKnowledgeSearch'
                        ? localize('com_tools_org_knowledge')
                        : localize('com_tools_title')
                    }
                </span>
            </div>
        </SelectTrigger>
```

修改文件`client/src/hooks/Messages/useSubmitMessage.ts`,
在`const submitMessage = useCallback(`中增加企业知识库选项的处理方式，即增加如下内容：

```
      if (data?.linsight) {
        setLinsightSubmission('new', {
       
        。。。。。。
        // 重置表单和清理草稿
        methods.reset();
        setFiles(new Map())
        setFilesToDelete({});
        helpers?.clearDraft && helpers.clearDraft();
        return;
      }

// 增加内容为
      const pro_knowledge_enabled = searchType && searchType === 'enterpriseKnowledgeSearch' || false;
      if(pro_knowledge_enabled){
          const _chatId = generateUUID(32)
          const flowId = 'bb89020ee1ac4d0c8d3b12234d0a85b9'
          const flowType = 10
          
          // 首先更新全局chatId状态，确保后续操作使用正确的chatId
          setChatId(_chatId)
          
          // 新建会话 - 使用与ChatView.tsx一致的完整流程
          queryClient.setQueryData<ConversationData>([QueryKeys.allConversations], (convoData) => {
            if (!convoData) {
                return convoData;
            }
            
            // 关键修复：正确设置conversation状态
            setConversation((prevState: any) => ({
                ...prevState,
                conversationId: _chatId,
                flowId,
                flowType,
                title: "知识库问答"
            }));
            
            return addConversation(convoData, {
                conversationId: _chatId,
                createdAt: new Date().toISOString(),
                endpoint: null,
                endpointType: null,
                model: "",
                flowId,
                flowType: flowType,
                title: "知识库问答",
                tools: [],
                updatedAt: new Date().toISOString()
            });
          });
          
          // 关键修复：先获取flow数据，再设置状态和导航
          getFlow(flowId).then(flowData => {
            // 设置当前对话状态，确保页面能正确显示
            queryClient.setQueryData([QueryKeys.conversation, _chatId], {
              conversationId: _chatId,
              flowId,
              flowType,
              title: "知识库问答",
              ...flowData
            });
            
            // 重置表单和清理草稿
            methods.reset();
            setFiles(new Map())
            setFilesToDelete({});
            helpers?.clearDraft && helpers.clearDraft();
            
            // 关键修复：先导航，等待页面加载完成后再设置提交数据状态
            navigate(`/chat/${_chatId}/${flowId}/${flowType}`, {
              state: {
                autoSubmitData: {
                  input: data?.text,
                  action: 'input',
                  chatId: _chatId,
                  flow: flowData,
                  files: chatFile,
                }
              }
            });
            
          }).catch(error => {
            console.error('获取流程数据失败:', error);
            // 处理错误情况，可以设置错误状态或显示错误信息
            setRunningState((prev) => ({
              ...prev,
              [_chatId]: {
                ...prev[_chatId],
                running: false,
                showStop: false,
                showUpload: true,
                inputDisabled: false,
                inputForm: true,
              },
            }))
          });
        return;
      }

 // 结束增加内容
      // 检查最新消息是否在会话中
      const rootMessages = getMessages();
      const isLatestInRootMessages = rootMessages?.some(
      。。。。。。     
```

在`export default function useSubmitMessage(helpers?: { clearDraft?: () => void }) {`
中增加
```
  const searchType = useRecoilValue(store.searchType);
  const { setConversation } = store.useCreateConversationAtom(0);
  const queryClient = useQueryClient();
  const navigate = useNavigate();
  const [_, setSubmitDataState] = useRecoilState(submitDataState)
  const [__, setRunningState] = useRecoilState(runningState)
  const [chatFile, setChatFileState] = useRecoilState(chatFileState)
  const chatState = useRecoilValue(currentChatState)
  // 添加chatId状态管理，与useAreaText.ts保持一致
  const [chatId, setChatId] = useRecoilState(chatIdState)
  const getFlow = async (id) => {
    const [flowRes] = await Promise.all([
      getFlowApi(id,  'v1')
    ])
    const flowData = { ...flowRes.data, isNew: false}
    return flowData
  }
```

增加引用为：
```

import { useQueryClient } from '@tanstack/react-query'
import { addConversation, generateUUID } from "~/utils"
import { ConversationData, QueryKeys } from "~/data-provider/data-provider/src"
import { useNavigate } from 'react-router-dom';
import { getFlowApi } from "~/api/apps";
import { chatFileState, chatIdState, currentChatState, runningState, submitDataState } from "~/pages/appChat/store/atoms";

```


### 修改markdown换行
修改文件`client/src/components/Chat/Messages/Content/Markdown.tsx`

0. 在
  ```
    export const p: React.ElementType = memo(({ children }: TParagraphProps) => {
  return <p className="mb-2 whitespace-pre-wrap">{children}</p>;
});
  ```
  之后增加代码,即：
  ```
  export const p: React.ElementType = memo(({ children }: TParagraphProps) => {
  return <p className="mb-2 whitespace-pre-wrap">{children}</p>;
});

// 处理HTML标签的自定义组件
export const html: React.ElementType = memo(({ children }: { children: React.ReactNode }) => {
  // 将HTML字符串转换为React元素
  if (typeof children === 'string') {
    // 处理<br>标签
    const processedContent = children
      .replace(/<br\s*\/?>/gi, '\n') // 将<br>标签转换为换行符
      .replace(/<[^>]*>/g, ''); // 移除其他HTML标签
    
    return <>{processedContent}</>;
  }
  
  return <>{children}</>;
});

// 处理表格单元格内的内容，专门处理<br>标签
export const td: React.ElementType = memo(({ children, ...props }: { children: React.ReactNode }) => {
  // 处理表格单元格内的内容
  const processTableCellContent = (content: React.ReactNode): React.ReactNode => {
    if (typeof content === 'string') {
      // 专门处理表格单元格内的<br>标签
      return content.replace(/<br\s*\/?>/gi, '\n');
    }
    
    // 如果是React元素，递归处理子元素
    if (React.isValidElement(content) && content.props.children) {
      return React.cloneElement(content, {
        ...content.props,
        children: processTableCellContent(content.props.children)
      });
    }
    
    return content;
  };
  
  return (
    <td {...props}>
      {processTableCellContent(children)}
    </td>
  );
});

const cursor = ' ';

  ```


1. 在
```
...
 components={
            {
              code,
              a,
              p,
              ....
```
中加入`td: TableCell`,`th: TableHeader`,
即：
```
....
components={
            {
              code,
              a,
              p,
              html, // 添加HTML标签处理
              td,   // 添加表格单元格处理
              artifact: Artifact,
              citation: ({ children }:
              ....
```

## 修改工作台中灵思为深度研究

要修改工作台中"灵思Linsight"为"深度研究"，需要修改以下翻译文件：

**主要修改文件：**
<mcfile name="translation.json" path="/Users/gujun/Downloads/bisheng-main/src/frontend/client/src/locales/zh-Hans/translation.json"></mcfile>

**具体位置：**
第678行，将：
```json
"com_segment_linsight": "灵思Linsight",
```
改为：
```json
"com_segment_linsight": "深度研究",
```

**同时需要修改的文件：**
<mcfile name="translation.json" path="/Users/gujun/Downloads/bisheng-main/src/frontend/client/src/locales/en/translation.json"></mcfile>

**英文版本位置：**
第723行，将：
```json
"com_segment_linsight": "Linsight",
```
改为：
```json
"com_segment_linsight": "Deep Research",
```

**前端平台翻译文件（如有）：**
<mcfile name="bs.json" path="/Users/gujun/Downloads/bisheng-main/src/frontend/platform/public/locales/zh/bs.json"></mcfile>

**位置：**
第505-507行，将相关描述中的"灵思"也修改为"深度研究"

这些文件包含了工作台界面中显示的翻译文本，修改后就能将"灵思Linsight"改为"深度研究"了。


#  Platform修改背景图片 



#  后端Backend需要修改的地方，增加依赖


0. 在backend的两个docker：`zgjy-backed`、`zgjy-backend-worker`中增加依赖

```
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple pdfplumber pandas numpy


```


